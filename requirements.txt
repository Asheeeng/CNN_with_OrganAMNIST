# ================================================================
# SimpleCNN_with_OrganAMNIST  依赖锁定文件
# Python 版本要求：  CPython 3.9.x  （本项目虚拟环境实际使用 3.9.6）
# 平台兼容：        macOS（Apple Silicon / Intel，走 MPS 或 CPU）
#                   Windows（x86_64，走 CPU；如需 NVIDIA CUDA 请见下方）
# 推荐使用：        venv / virtualenv  创建独立虚拟环境后安装
# ================================================================
#
# 【快速上手】（任一平台通用）
#   1) 以 Python 3.9 创建虚拟环境
#        macOS/Linux:  python3.9 -m venv .venv  &&  source .venv/bin/activate
#        Windows:      py -3.9 -m venv .venv    &&  .venv\Scripts\activate
#   2) 升级 pip（避免旧 pip 解不出新版 torch / numpy）
#        python -m pip install --upgrade pip
#   3) 安装依赖
#        pip install -r requirements.txt
#
# 【关于 torch 的 CUDA 加速（仅 Windows + NVIDIA 显卡用户）】
#   默认以下 torch / torchvision 为 通用 CPU/MPS 版（macOS + Windows 都能跑）。
#   Windows 如需 CUDA，请在 安装完其它依赖后 单独重装 PyTorch：
#       (CUDA 12.1)
#       pip install torch==2.8.0 torchvision==0.23.0 --index-url https://download.pytorch.org/whl/cu121
#
# 【直接依赖（源码实际 import）】
#   torch, torchvision, medmnist, matplotlib, scikit-learn, sympy, numpy
#   其余均为以上包自动带进来的"传递依赖"，已一并锁版本便于 100% 复现。
# ================================================================

# --- 核心深度学习 ------------------------------------------------------
torch==2.8.0
torchvision==0.23.0

# --- 数据集 -----------------------------------------------------------
medmnist==3.0.2

# --- 科学计算 / 机器学习 ----------------------------------------------
numpy==2.0.2
scipy==1.13.1
scikit-learn==1.6.1
sympy==1.14.0
pandas==2.3.3

# --- 绘图 -------------------------------------------------------------
matplotlib==3.9.4
pillow==11.3.0

# --- 图像处理（medmnist 传递依赖） -------------------------------------
scikit-image==0.24.0
ImageIO==2.37.2
tifffile==2024.8.30
lazy-loader==0.5

# --- 通用工具（matplotlib / pandas / torch 等的传递依赖） ---------------
contourpy==1.3.0
cycler==0.12.1
filelock==3.19.1
fire==0.7.1
fonttools==4.60.2
fsspec==2025.10.0
importlib_resources==6.5.2
Jinja2==3.1.6
joblib==1.5.3
kiwisolver==1.4.7
MarkupSafe==3.0.3
mpmath==1.3.0
networkx==3.2.1
packaging==26.3
pyparsing==3.3.2
python-dateutil==2.9.0.post0
pytz==2026.3.post1
six==1.17.0
termcolor==3.1.0
threadpoolctl==3.6.0
tqdm==4.70.0
typing_extensions==4.16.0
tzdata==2026.3
zipp==3.23.1
